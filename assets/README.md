# `assets/` — legacy mixed content

This directory currently contains assets from multiple concerns and is scheduled for
classification and extraction.

- Do not add new project deliverables here.
- Move project-owned assets to `../projects/<project>/assets` during migration.
- Keep only intentionally reusable MoonieX product assets in the final core repository.
