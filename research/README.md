# `research/` — legacy personal/project content

Existing research is pending extraction from the shared control-plane repository.

- Private research belongs in `../mooniex-private`.
- Project research belongs with its project.
- Only durable product architecture research should be promoted into `docs/`.
