---
description: Spawn the CTO org session (iTerm window, CTO chat tab)
argument-hint: "[--new | --last | --resume <id> | --with-logs]"
allowed-tools: Bash(bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cto.sh:*)
---
Spawn the **CTO** C-level session now. Forward any flags the user passed (`$ARGUMENTS`):

!`bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cto.sh $ARGUMENTS`

Reply in ONE line: role (CTO) + session id from the script output. Nothing else.
