---
description: Spawn the CGO org session (iTerm window, CGO chat tab)
argument-hint: "[--new | --last | --resume <id> | --with-logs]"
allowed-tools: Bash(bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh:*)
---
Spawn the **CGO** C-level session now. Forward any flags the user passed (`$ARGUMENTS`):

!`bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh --role cgo $ARGUMENTS`

Reply in ONE line: role (CGO) + session id from the script output. Nothing else.
