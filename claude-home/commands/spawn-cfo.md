---
description: Spawn the CFO org session (iTerm window, CFO chat tab)
argument-hint: "[--new | --last | --resume <id> | --with-logs]"
allowed-tools: Bash(bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh:*)
---
Spawn the **CFO** C-level session now. Forward any flags the user passed (`$ARGUMENTS`):

!`bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh --role cfo $ARGUMENTS`

Reply in ONE line: role (CFO) + session id from the script output. Nothing else.
