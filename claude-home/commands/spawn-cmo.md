---
description: Spawn the CMO org session (iTerm window, CMO chat tab)
argument-hint: "[--new | --last | --resume <id> | --with-logs]"
allowed-tools: Bash(bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh:*)
---
Spawn the **CMO** C-level session now. Forward any flags the user passed (`$ARGUMENTS`):

!`bash /Users/gob/MoonieXHQ/Agents/Core/scripts/spawn-cxo.sh --role cmo $ARGUMENTS`

Reply in ONE line: role (CMO) + session id from the script output. Nothing else.
