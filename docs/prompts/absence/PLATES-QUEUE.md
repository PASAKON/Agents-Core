# PLATE QUEUE — authoritative, 2026-08-28 17:36

My tmux relays have been arriving fragmented. **This file is the source of truth.**

## DONE — do not redo

- `prop_cart_b` · `bc89ef6c` ✅
- `char_woman_c` · `c677a65d` ✅

## 1 · `loc_wall_pov_d` — FILE THE BEST ONE YOU ALREADY HAVE

**The CEO relaxed the bar at 17:10: "ขอเหมือนที่สุดก็พอ ประมาณ 10-20% Error ได้ไม่ว่ากัน".**

The crack does **not** have to match pixel for pixel. It has to **read** as the
same crack to someone watching for a few seconds — roughly the same length,
roughly the same diagonal path, roughly the same branching, mirrored. That is the
entire test.

**Go back through the attempts you already fired, pick the closest, and file it.**
Do not fire again for the crack alone.

Two things are still non-negotiable, because a miss breaks a scene rather than
looking slightly off:

- **ZERO PEOPLE anywhere in frame.** Faces are what the Face/IP scanner kills —
  it has killed three plates today.
- **A LARGE RED DOOR** at the far end, dead opposite the camera, the room's only
  entrance, wide enough for a wheelchair.

Fire again **only** if your best attempt is missing the door or contains a person.

Download → `.../Sorry, Sir/Element/Location/absence-loc-wall-pov-d.png`.
Report the asset id **and the exact Element mention string**.

## 2 · `loc_hall_big_e` — the same door, seen from inside

Bind `project_absence_loc_hall_big_d` and change **only one thing**: add the same
red door at the far end, matching `loc_wall_pov_d` in size, colour and position.
Keep the chromium trumpet columns, the warm orange cove lighting, the terracotta
terrazzo floor, the artworks on plinths, the wall proportions. **No people.**

The two frames are the same room from opposite sides — if the door disagrees
between them, the geometry of every scene breaks.

New Element, never re-point the old one.

## 3 · `loc_mansion` v2 — Dupe's house with the new cart

Bind the existing `loc_mansion` and `prop_cart_b`. Everything stays: sunken plum
seating, warm wood, tall windows, impersonal luxury. **Only the cart in the corner
changes** — it is now `prop_cart_b`, with the painting slotted upright in its
rack, gold V still on the front panel. **No people.**

New Element `loc_mansion_b`.

## SETTINGS

GPT Image 2 · 16:9 · 2K · **Medium**. ~2.5 credits is correct and expected. If it
reads ~8.5 the Quality is on High — switch it. The UNLIMITED badge beside GPT
Image 2 is a **$30 upsell advertisement, not a state** — never click it. Never
click Rerun. **Commit every asset id before downloading** and put them all in your
report body.
