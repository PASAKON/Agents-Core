1. GOAL: new tests in `scripts/test_tmux_send_keys_settle.py` pass and plain `pytest` stays green: `tools.tmux_session.send_keys(session, text)` types the text, waits, then submits with a settle delay and a rescue Enter — the same sequence `tools/agent_transport.py::_wake_tmux_send` already uses — and `send_keys(..., press_enter=False)` only types.
2. FILES you may touch: `tools/tmux_session.py`, `scripts/test_tmux_send_keys_settle.py` (new). No other file (do NOT edit tools/agent_transport.py).
3. FORBIDDEN: running real tmux in tests (monkeypatch `subprocess.run`/the module's `_run`, `time.sleep`, `has_session`, `tmux_bin`); editing existing tests; scratch/log files; dependency edits.
4. WHAT IS KNOWN (verified 2026-09-23 on main): `send_keys()` (tools/tmux_session.py ~line 147) runs `tmux send-keys -t <s> -l <text>` then `tmux send-keys -t <s> C-m` back-to-back with zero delay (GitHub issue #70: suspected to hit the TUI's bracketed-paste Enter-swallow). `tools/agent_transport.py::_wake_tmux_send` (~line 129) already does the proven sequence: `-l <text>`, `time.sleep(0.4)`, `Enter`, `time.sleep(0.3)`, `Enter` again as a rescue — mirror exactly that (use the key name `Enter`, not `C-m`). Keep the `has_session` check and the `-l` literal mode. Today the only callers of send_keys are the module's own CLI (`tmux_session.py` `__main__`); agent_transport deliberately bypasses it.
5. ENV NOTE: pytest collects `scripts lib tests` (checked today); new test in `scripts/`; use `assert`. Report anything missing on your VM; do not install system packages.
6. DELIVERABLE: one PR titled "tmux_session.send_keys: settle delay + rescue Enter, same as _wake_tmux_send (#70)"; description = files, the call sequence the test pins, pytest summary line. Put "Closes #70".
7. FACTS, NOT GUESSES: cite output; anything not established is "unknown — not verified".
8. No questions needed; proceed. If a fact above is wrong, say which in the PR and continue with the correct one.
