1. GOAL: on branch `bootstrap/landing-mvp`, `pnpm tsc --noEmit` and `pnpm test` pass (what `.github/workflows/test.yml` runs), and a new test `tests/unit/components/office/TransportPrototypeScene.test.ts` proves the scene emits `"ready"` on its bus after it has subscribed its listeners.
2. FILES you may touch: `src/components/office/TransportPrototype.tsx`, `src/components/office/phaser/scenes/TransportPrototypeScene.ts`, `tests/unit/components/office/TransportPrototypeScene.test.ts` (new). No other file.
3. FORBIDDEN: dependency or lockfile edits (no jsdom, no testing-library, no phaser version change); editing existing tests; scratch/log files; touching other Phaser scenes (OfficeScene, Worker, CameraController are fine — their `.once` calls run after boot); changing the component's error logging.
4. WHAT IS KNOWN (verified 2026-09-23 on `bootstrap/landing-mvp`): Sentry issue JAVASCRIPT-NEXTJS-NU-1H "Cannot read properties of undefined (reading 'once')" on the admin transport page, first seen 2026-06-01 — the day `TransportPrototype.tsx` was added. In `initGame()` (~line 136-147) the component does `const scene = new TransportPrototypeScene(bus);` and then `scene.events.once(Phaser.Scenes.Events.CREATE, …)` BEFORE `new Phaser.Game({… scene …})`. In Phaser 3.90 `Scene.events` is only declared in the constructor and is assigned when the Game boots the scene (Systems.init → EventEmitter plugin), so for a scene constructed with `new` and not yet added to a Game, `scene.events` is undefined and that line throws. Fix without touching Phaser internals: the scene already receives the bus in its constructor (`this.bus`); at the END of `create()`, after `this.bus.on("seed" | "delivery" | "cost", …)`, emit `this.bus.emit("ready")`. In the component, replace the `scene.events.once(CREATE, …)` block with `bus.once("ready", () => { if (!mounted) return; bus.emit("cost", latestRef.current.cost); bus.emit("seed", latestRef.current.deliveries); })`, registered before `new Phaser.Game`. Test: `vitest.config.ts` uses environment "node"; Phaser needs a browser, so `vi.mock("phaser", …)` with a minimal `Scene` base class (constructor accepting a config; `cameras.main.setBackgroundColor`, `add.graphics/text/…`, `time.addEvent`, `events.once` as no-op stubs, as create() needs) and a real `Events.EventEmitter` substitute (e.g. node's `events.EventEmitter`); construct the scene with a bus, call `create()`, assert the bus's "ready" listener fired and that "seed"/"cost" listeners were attached before it.
5. ENV NOTE: package manager is pnpm (`pnpm install --frozen-lockfile`); Node 20. If your VM cannot install or run something, report it in the PR — do not change dependencies. Use `assert`/`expect`, never a test that passes whatever happens.
6. DELIVERABLE: one PR against `bootstrap/landing-mvp` titled "fix(transport-prototype): wait for the scene's ready signal instead of scene.events before boot (Sentry 1H, #74)"; description = root cause, files, the tsc + test output lines. Mention "Fixes item 1 of #74" (do NOT write "Closes #74" — items 2 and 3 are not code).
7. FACTS, NOT GUESSES: cite command output and file:line; anything not established is "unknown — not verified".
8. No questions needed; proceed. If a fact above is wrong, say which in the PR and continue with the correct one.
