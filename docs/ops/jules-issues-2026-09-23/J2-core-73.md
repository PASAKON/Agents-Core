1. GOAL: new tests in `scripts/test_self_repo_guard_flags.py` pass and plain `pytest` stays green: `bash_targets()` in `scripts/hook-self-repo-guard.py` returns the protected path for `rm -f state/tasks.db`, `rm -f lib/db.py`, `touch state/tasks.db`, `touch lib/x.py`, `cp -f src.txt lib/x.py`, while `sed -f script.sed lib/x.py` still does NOT count `script.sed` as a target and `truncate -s 0 state/tasks.db` still returns `state/tasks.db`.
2. FILES you may touch: `scripts/hook-self-repo-guard.py`, `scripts/test_self_repo_guard_flags.py` (new). No other file.
3. FORBIDDEN: editing or loosening any existing test (scripts/test_self_repo_guard.py, scripts/test_hook_self_repo_guard.py stay as they are and must still pass); changing PROTECTED_* sets; scratch/log files; dependency edits.
4. WHAT IS KNOWN (verified 2026-09-23 by calling the function on main): `bash_targets("rm -f state/tasks.db") == []`, `bash_targets("rm -f lib/db.py") == []`, `bash_targets("touch state/tasks.db") == []`, `bash_targets("cp x -f state/tasks.db") == ['x']`, while `bash_targets("rm state/tasks.db") == ['state/tasks.db']` and `bash_targets("rm -rf state/tasks.db")` works. Two causes: (a) `_FLAG_WITH_VALUE` is ONE global set used by `_positional()` for every command; it holds `-f`/`-t`/`-s`/`-S` because they take a value for `sed`/`cp`/`install`/`truncate`, so for `rm -f X` the path X is skipped as "the value of -f"; (b) `touch` is not in `_ARG_DESTRUCTIVE`. Fix: make value-taking flags per command (e.g. a dict command -> set of value flags, empty for `rm`), pass the command into `_positional`, and add `touch`. Source: GitHub issue #73 (it named only the tasks.db case; the hole is wider).
5. ENV NOTE: pytest collects `scripts lib tests` (pytest.ini, checked today); put the new test in `scripts/`; use `assert`; load the hook module with `importlib.util.spec_from_file_location` (hyphenated filename). Your VM lacking a package -> report it; CI installs requirements.txt.
6. DELIVERABLE: one PR titled "self_repo_guard: per-command value flags + touch — rm -f / touch no longer slip past (#73)"; description = root cause, files, the pytest summary line, and before/after of the bash_targets calls above. Put "Closes #73".
7. FACTS, NOT GUESSES: cite output; anything not established is "unknown — not verified".
8. No questions needed; proceed. If a fact above is wrong, say which in the PR and continue with the correct one.
