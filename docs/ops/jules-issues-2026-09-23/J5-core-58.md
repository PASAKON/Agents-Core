1. GOAL: `scripts/gdrive-bridge/ilag_mirror.py` refuses to run when `<where>` does not match the Drive folder it would upload into. Before a single upload it fetches the folder's own name for `folder_id`, compares it with the last path segment of `where` (`"All Scene/S9-C"` → `S9-C`, a trailing `/` ignored), and on mismatch prints both names and returns exit code 2 with NO upload, NO `append_log`, NO delete. A failed metadata lookup (any exception) also returns 2 with nothing uploaded. New tests in `scripts/test_ilag_mirror.py` pass and plain `pytest` stays green.
2. FILES you may touch: `scripts/gdrive-bridge/ilag_mirror.py`, `scripts/test_ilag_mirror.py` (new). No other file.
3. FORBIDDEN: changing `list_folder`'s name or signature (imported by `runners/drive_upload_broker.py:102` and `runners/drive_photo_broker.py:141`; `scripts/test_drive_photo_broker.py:267` asserts `from ilag_mirror import list_folder`); editing `scripts/gdrive-bridge/ilag_sync.py`; changing the delete default or the `--no-delete` flag; any real network call in tests; scratch/log files; dependency edits.
4. WHAT IS KNOWN (read on main 2026-09-23): `main()` parses `folder_id`, `where`, `note`, `files`, `--actor`, `--no-delete`; checks local files exist (returns 1 if missing); then calls `upload(p, p.name, args.folder_id)` per file; re-lists with `list_folder(args.folder_id)`; writes `args.where` only into the log line via `append_log`; then `p.unlink()` unless `--no-delete`. Nothing compares `where` with `folder_id` — GitHub issue #58: 3 Scene 9-C clips landed in Scene 3's folder, the log said 9-C, and the local copies were deleted. The Drive helper is `ilag_sync.api(url, *, method="GET", params=None, ...)` returning parsed JSON, and `DRIVE_FILES = "https://www.googleapis.com/drive/v3/files"` is already imported; one folder's metadata is `api(f"{DRIVE_FILES}/{folder_id}", params={"fields": "id,name"}, timeout=60)` → `{"id": ..., "name": ...}`. Put the lookup in a small new function (e.g. `folder_name(folder_id) -> str`) so tests can monkeypatch it. Run the check after the missing-file check and before the upload loop.
5. ENV NOTE: pytest collects `scripts lib tests` (pytest.ini, checked today); use `assert`. Import the script like `scripts/test_drive_photo_broker.py` does: `sys.path.insert(0, str(Path(__file__).resolve().parent / "gdrive-bridge"))` then `import ilag_mirror`. Monkeypatch `ilag_mirror.upload`, `ilag_mirror.list_folder`, `ilag_mirror.append_log`, `ilag_mirror.folder_name` (or `ilag_mirror.api`) and `sys.argv`; use real files under `tmp_path`. Cover: mismatch → 2, upload/append_log never called, local file still exists; match → upload called, log line carries `where`, file deleted; match + `--no-delete` → file kept; lookup raises → 2, nothing uploaded; `where` with trailing `/` matches. Your VM lacking a package → report it; CI installs requirements.txt.
6. DELIVERABLE: one PR titled "ilag_mirror: refuse when <where> does not name the target folder — no upload, no delete (#58)"; description = root cause, files, the pytest summary line, and the refusal message text. Put "Closes #58".
7. FACTS, NOT GUESSES: cite output; anything not established is "unknown — not verified".
8. No questions needed; proceed. If a fact above is wrong, say which in the PR and continue with the correct one.
