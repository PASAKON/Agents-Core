# RTK org-wide adoption A/B — 2026-09-25 (task-9ea68924)

## Question

Should MoonieX HQ wire [rtk-ai/rtk](https://github.com/rtk-ai/rtk) v0.50.0 (a
PreToolUse hook that rewrites recognized Bash commands to compact-output
wrappers) into every project org-wide?

## Decision rule (CEO-approved plan 2026-09-25)

**ADOPT** if arm B (RTK) uses **≤ 0.8×** arm A's total tokens (cache read +
cache write + fresh input + output, deduped by `message.id`) with **equal
graded correctness** (10/10 on the same answer key) on **≥ 2 of 3** paired
runs. Otherwise **DO NOT ADOPT**.

## Method

- **Fixture:** `mooniex-agents` at commit `472466a9` (this task's branch base
  = `origin/main` at kickoff), checked out twice via `git worktree add
  --detach` into `tools/rtk_ab/arms/{A,B}` (gitignored), sparse-checked-out
  (cone mode) to `tools scripts tests lib .claude` — a full checkout is
  916 MB (tracked-blob sum), sparse is ~11 MB per arm. `.claude/` (tracked:
  `settings.json` + 5 project PreToolUse hooks + `skills/`) is kept in **both**
  arms so the org's existing hooks apply identically; the only difference
  between A and B is arm B's own `.claude/settings.local.json` (below).
- **Job:** `tools/rtk_ab/PROMPT.md` — 10 read-only questions (pytest counts on
  `tests/test_restore_scripts.py`, a `skill(`-prefix count over `git log
  --oneline -30`, the 5 largest files under `tools/`, `.py` counts under
  `scripts/`/`lib/`, `def main(`/`import argparse` grep counts under
  `tools/`, an `ls -R scripts` directory-header count, a `git show --stat
  HEAD` fact, `wc -l tools/token_profile.py`) — deliberately mixed simple
  commands (`git log`, `git show`, `wc -l`) RTK recognizes and compound/piped
  ones (`find … | wc -l`, `grep -l … | wc -l`, `ls -R … | grep -c`) it
  doesn't, per the survey's prediction that most of this org's real Bash is
  exactly the latter. Answers computed by hand with plain shell into
  `ANSWER_KEY.json` before any run; `grade.py` scores a run's final JSON line
  against it, out of 10.
- **Runs:** `tools/rtk_ab/run_ab.py`, `claude -p "<PROMPT>" --model
  claude-sonnet-5 --output-format json --permission-mode bypassPermissions`,
  cwd = the arm dir, 20 min timeout. 1 smoke run per arm first, then 3 paired
  measured runs alternating start order (i=1 A,B · i=2 B,A · i=3 A,B) — 8
  headless calls total (budget cap).
- **Measurement:** per run, `tools/token_profile.py`'s `scan()` against the
  run's transcript `~/.claude/projects/<slug>/<session_id>.jsonl` — tokens by
  class deduped by `message.id` (a raw sum overcounts ~2×), plus turns and
  Bash tool-result chars from the same scan.

## Hook wiring that worked

`rtk init` (no `-g`) for Claude Code only writes `CLAUDE.md` prose + a filter
template — no hook. RTK's real interception is documented as `rtk init -g`
only (writes `~/.claude/settings.json`), which is forbidden here (never touch
global settings). Claude Code's hook schema doesn't care who writes it, so
this JSON was hand-verified against `rtk hook claude`'s real stdin/stdout
contract and placed in **arm B's own** `.claude/settings.local.json` only:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/gob/MoonieXHQ/Agents/Core/worktrees/mooniex-agents__developer__task-9ea68924/tools/rtk_ab/bin/rtk hook claude"
          }
        ]
      }
    ]
  }
}
```

Verified: piping a simulated PreToolUse payload for `git log --oneline -30`
into `rtk hook claude` returns clean JSON on stdout —
`{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecisionReason":"RTK auto-rewrite","updatedInput":{"command":"rtk git log --oneline -30", ...}}}`
— and an unrecognized/compound command (`python -m pytest ...`, any piped
command) returns **empty stdout**, Claude Code's "no change" convention.

## Install

```bash
gh release download v0.50.0 --repo rtk-ai/rtk \
  --pattern 'rtk-aarch64-apple-darwin.tar.gz' --pattern 'checksums.txt' --dir /tmp/rtk_dl
shasum -a 256 /tmp/rtk_dl/rtk-aarch64-apple-darwin.tar.gz   # matched checksums.txt
tar -xzf /tmp/rtk_dl/rtk-aarch64-apple-darwin.tar.gz -C /tmp/rtk_dl
cp /tmp/rtk_dl/rtk tools/rtk_ab/bin/rtk && chmod +x tools/rtk_ab/bin/rtk
tools/rtk_ab/bin/rtk --version   # rtk 0.50.0
```

`brew install rtk` (homebrew-core, stable 0.50.0) is a legitimate fallback —
not used here, to avoid a global brew-state change for an experiment.

## Bug found by the smoke run

RTK's rewrite emits the bare literal `rtk` as argv[0] (not its own install
path). `tools/rtk_ab/bin` isn't on the default `PATH`, so **every** rewritten
command in the first B smoke run failed `exit 127: command not found: rtk`.
The tested agent never saw an RTK-shaped error — just commands that
mysteriously failed — and burned many extra turns working around it: it
fell back to `/bin/ls` with an absolute path, `cat`'d `.git` three times,
tried 5 separate ways to get `wc -l tools/token_profile.py` to work (four
failed silently with exit 127 before one redirect form happened to dodge the
rewrite). Net effect: the broken smoke run for B used **more** than double
A's tokens (3.92M vs 1.76M) and 42 turns vs 20 — the opposite of RTK's
purpose, purely from a missing `PATH` entry. **Fix** (before any measured
run): `run_ab.py` prepends `tools/rtk_ab/bin` to the child `claude` process's
`PATH` (a no-op for arm A). Mirrors RTK's own README: "Installs to
`~/.local/bin`. Add to PATH if needed." **The smoke runs are excluded from
the decision table below** — they're a pre-flight check, and B's smoke number
in particular reflects the PATH bug, not RTK's real behavior.

## Results

All 6 measured runs scored **10/10** (equal graded correctness — the decision
rule's correctness gate is satisfied on every pair; the rule turns entirely on
the token ratio). Token columns are deduped by `message.id` via
`token_profile.scan()`. "Bash chars" = total chars of Bash tool-result output
(a proxy for how much output text each arm's model had to read back).

| pair i | order | run | arm | score | turns | fresh-in | cache-write | cache-read | output | **total tokens** | Bash chars | wall (s) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | A,B | A-1 | A | 10/10 | 20 | 40 | 71,616 | 1,712,255 | 6,447 | **1,790,358** | 13,429 | 101.1 |
| 1 | A,B | B-1 | B | 10/10 | 14 | 28 | 66,021 | 1,151,693 | 4,613 | **1,222,355** | 6,768 | 102.1 |
| 2 | B,A | B-2 | B | 10/10 | 24 | 48 | 66,967 | 2,028,948 | 4,241 | **2,100,204** | 8,297 | 142.9 |
| 2 | B,A | A-2 | A | 10/10 | 12 | 24 | 66,552 | 976,921 | 4,270 | **1,047,767** | 7,473 | 72.2 |
| 3 | A,B | A-3 | A | 10/10 | 12 | 24 | 66,558 | 963,389 | 4,282 | **1,034,253** | 7,580 | 92.4 |
| 3 | A,B | B-3 | B | 10/10 | 20 | 40 | 69,016 | 1,680,851 | 6,400 | **1,756,307** | 8,897 | 103.6 |

**Ratio B/A per paired run i** (decision rule: PASS if ≤ 0.8):

| pair | A total | B total | ratio B/A | vs. 0.8 threshold |
|---|---|---|---|---|
| i=1 | 1,790,358 | 1,222,355 | **0.68** | PASS |
| i=2 | 1,047,767 | 2,100,204 | **2.00** | FAIL |
| i=3 | 1,034,253 | 1,756,307 | **1.70** | FAIL |

**1 of 3 pairs pass.** The rule requires ≥ 2 of 3.

**Why the swing, and why it isn't RTK:** the same read-only job took anywhere
from 11 to 24 Bash calls depending on the run, in *both* arms — driven almost
entirely by how much the model had to grope for the right Python before it
could run `tests/test_restore_scripts.py` (this fixture has no active venv on
`PATH`; the model tries `python3 -m pytest`, discovers there's no `pytest`
module there, then searches for a `.venv`, `find / -maxdepth 6 -iname pytest`,
etc., before landing on `/Users/gob/MoonieXHQ/Agents/Core/.venv/bin/python`).
Env-discovery probes (`find /`, `.venv`, `which python`) appear in every one
of the 6 runs: A-1 10, A-2 7, A-3 8, and (checked directly in the Bash-call
list) throughout B-1/B-2/B-3 too. That churn, not the presence or absence of
RTK, is what actually decided each run's token count. i=1's 0.68 looks like a
win for B, but B-2 and B-3 lost by even larger margins with the identical
mechanism (or lack of it) at play — see below.

## Verdict

**DO NOT ADOPT.** 1 of 3 paired runs meets the ≤0.8× threshold; the rule
requires ≥ 2 of 3. The correctness gate was never the issue (6/6 runs scored
10/10) — the token side of the rule fails, and the reason it fails is stronger
than "unlucky sampling": in the properly-configured measured runs (PATH fix
applied), **RTK's hook rewrote zero Bash commands in all three arm-B runs.**
Direct count from each run's transcript:

| run | total Bash calls | calls RTK rewrote (bare `rtk …`) |
|---|---|---|
| B-1 | 13 | **0** |
| B-2 | 23 | **0** |
| B-3 | 19 | **0** |

57 Bash calls, 0 rewrites. Every single command the tested agent issued in
arm B was compound, piped, or `cd`-prefixed (`pwd && git rev-parse
--show-toplevel`, `find ... 2>/dev/null | grep -v Trash | head`, `cd
<abs-path>\necho ...`, `git log --oneline -30 | grep -cE '^[0-9a-f]+
skill\('`) — exactly the shape `rtk hook claude` returns empty stdout for
(confirmed directly against the hook in "Hook wiring" above: only a bare
single recognized command like `git log --oneline -30` gets rewritten). So
the token deltas between A and B in every pair are 100% attributable to
ordinary run-to-run model variance in how much env-discovery churn happened,
not to RTK doing anything, in either direction. RTK was mechanically live
(hook wired, binary on PATH, no exit-127s) and simply never had a matching
input to act on.

## What RTK dropped that mattered

**Nothing — because in the corrected (PATH-fixed) measured condition, RTK
never fired at all.** Its only *measured* effect across this whole
experiment was negative, and it happened in the excluded smoke run: RTK
rewrote commands to bare `rtk <subcommand>`, which wasn't resolvable on the
default `PATH`, so B-0 silently failed 127 on every rewritten command and the
agent burned 42 turns / 3.92M tokens working around invisible failures vs.
A-0's 20 turns / 1.76M (see "Bug found by the smoke run" above). Once that's
fixed, RTK doesn't drop anything the tested agent needed, because it doesn't
touch anything the tested agent actually writes: this org's real Bash usage
(here and, per `tools/token_profile.py`'s own survey basis, across the wider
codebase) is dominated by `&&`/`;`/pipes/`cd`-prefixes/python heredocs — the
exact set RTK's own hook contract passes through unchanged (empty stdout).

## Org-wide rollout recipe (or reason not to)

**Do not roll RTK out org-wide.** Two independent reasons, either one
sufficient on its own:

1. **Fails the CEO-approved decision rule outright** — 1 of 3 paired runs
   passes the ≤0.8× token threshold, not the required ≥2 of 3.
2. **The mechanism doesn't engage with how this org's agents write Bash.**
   0 of 57 Bash calls across the 3 measured arm-B runs were in a form RTK
   rewrites. A tool that never fires can't produce savings, and — as the
   smoke run showed — if it's ever misconfigured (e.g. an install dir not on
   `PATH`) it can actively cost more than double the tokens of not having it
   at all, because its failures are silent (exit 127, no RTK-labeled error)
   and the agent burns turns working around what looks like a broken
   environment rather than a broken hook.

If RTK's authors later ship compound/piped-command rewriting (the gap that
made it a no-op here), this verdict is worth re-running — the harness in
`tools/rtk_ab/` is reusable as-is (swap the binary, rerun `--smoke` then
`--measured`).
