GitHub issue #65: send_to_dev's fallback tab lookup matches only the first 6 characters of the task id (5 of which are always "task-"), so messages can land in the wrong DEV's tab. Check whether it's still there, fix it if so, add a test, open a PR.
