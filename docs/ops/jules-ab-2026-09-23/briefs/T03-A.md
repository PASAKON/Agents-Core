1. GOAL: `pytest tests/test_close_dev_releases_locks.py` (new) passes and `pytest` stays green. The test proves that `tools.worker_reap.close_dev` releases every row in the `locks` table owned by the task, on the remote-host path as well as the local one.
2. FILES you may touch: `tools/worker_reap.py`, `tests/test_close_dev_releases_locks.py` (new) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: changing `lib/db.py` — it already has `release_task_locks(task_id, project)` and it works.
4. KNOWN LEAD (GitHub issue #159, measured 2026-09-22): `task-54ff705b` (host=winbox, browser_operator) was closed with `close_dev(status="done")` cleanly and the task row went to `done`, but its two path locks stayed in `locks` and the next task on the same path was refused. `lib.db.release_task_locks('task-54ff705b', 'mooniex-agents')` released both. So `close_dev` skips the release on the remote path. Find the branch in `close_dev` that handles remote/winbox tasks and make every close path release locks. Build the test on the repo's existing sqlite test fixtures (see tests/conftest.py and how neighbouring tests create a task and locks) — never a real tasks.db.
5. ENV NOTE: tests that need tmux, iTerm or Windows are skipped or mocked in this repo; if something you need is missing on your VM, report it — do not install system packages.
6. DELIVERABLE: one PR titled "fix(close_dev): release path locks on the remote path too (#159)"; description = root cause, files, the pytest summary line; reference #159.
7. No questions needed; proceed.
