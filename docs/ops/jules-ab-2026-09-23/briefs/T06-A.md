1. GOAL: `pytest tests/test_watchdog_plist.py` (new) passes and `pytest` stays green. The test loads `scripts/com.mooniex.agents-watchdog.plist` with `plistlib` and asserts `EnvironmentVariables` exists with `PATH` starting `/opt/homebrew/bin:/usr/local/bin:` and containing `/usr/bin:/bin`, and `ORG_WATCHDOG_BRANCH_POLL` equal to "0".
2. FILES you may touch: `scripts/com.mooniex.agents-watchdog.plist`, `tests/test_watchdog_plist.py` (new) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: changing ProgramArguments, Label or any interval in the plist; touching runners/watchdog.py.
4. KNOWN LEAD (GitHub issue #155, measured 2026-09-18): the launchd daemon runs with no `EnvironmentVariables` block, so `gh` (installed at /opt/homebrew/bin/gh) is "not found" and every stalled-worker issue it tries to file fails silently; `ORG_WATCHDOG_BRANCH_POLL` is never set, so the branch-poller pass stays off by accident instead of by decision. Add the block; keep `ORG_WATCHDOG_BRANCH_POLL` at "0" (the CEO has not ruled on turning it on) with an XML comment saying so.
5. ENV NOTE: launchd does not exist on your VM; the test only parses the file. Report anything else missing.
6. DELIVERABLE: one PR titled "watchdog: launchd EnvironmentVariables — PATH with /opt/homebrew/bin, ORG_WATCHDOG_BRANCH_POLL=0 (#155)"; description = root cause, files, pytest summary line; reference #155.
7. No questions needed; proceed.
