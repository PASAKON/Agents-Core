1. GOAL: `pip install -r requirements.txt && pytest` must pass on a fresh Python 3.12 environment where pip currently resolves `mcp` 2.x. Today it fails at collection with `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` in scripts/test_cto_mcp_server.py, scripts/test_dev_message.py, scripts/test_relay_mcp_server.py and scripts/test_tool_parity.py.
2. FILES you may touch: `requirements.txt` only — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: editing any runner or test to work around the import, and migrating to `mcp.server.mcpserver` (that is a separate task).
4. KNOWN LEAD: `mcp` is not pinned — it arrives transitively via `claude-agent-sdk`. mcp 2.0 renamed FastMCP to MCPServer, so the three importers (runners/cto_mcp_server.py, runners/relay_mcp_server.py, runners/worker_mcp_server.py) break on 2.x. The fix is a floor+ceiling pin `mcp>=1.27,<2` in requirements.txt with a 2-line comment saying why (CI collection broke 2026-09-22) and that the migration is tracked separately.
5. ENV NOTE: if your VM cannot install something from requirements.txt (e.g. psycopg wheels), say so in the PR description; do not change that line. CI installs everything.
6. DELIVERABLE: one PR titled "deps: pin mcp<2 — mcp 2.0 renamed FastMCP, CI collection broke 2026-09-22"; description = root cause (2 lines), the file changed, and the exact pytest summary line you got.
7. No questions needed; proceed.
