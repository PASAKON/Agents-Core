1. GOAL: `pytest tests/test_jules.py` (new) passes, `pytest` stays green, and `python tools/jules.py --help` exits 0.
2. FILES you may touch: `tools/jules.py` (new), `tests/test_jules.py` (new), `docs/ops/jules-client.md` (new, at most 40 lines of usage) — no other file. You may READ `tools/jules_batch.py` for the API shapes but must not modify it.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: new dependencies (standard library only: urllib, json, argparse, pathlib); network calls in tests (monkeypatch the HTTP layer); printing or logging the API key.
4. SPEC: a CLI for the Jules API — base `https://jules.googleapis.com/v1alpha`, header `X-Goog-Api-Key`, key read at runtime from `~/.config/mooniex/jules.env` line `JULES_API_KEY=...` (path overridable by env `JULES_ENV_FILE`). Subcommands: `create --repo <name> --title <t> --brief <file> [--branch main]` → POST /sessions with sourceContext.source "sources/github/PASAKON/<repo>", automationMode AUTO_CREATE_PR, requirePlanApproval false, prints the session id; `status <id>` → state + PR url(s) from outputs[].pullRequest.url; `diff <id> [--out f]` → the LAST artifacts[].changeSet.gitPatch.unidiffPatch across all pages of GET /sessions/<id>/activities; `report <id>` → every activity's createTime + kind + message text; `gate <id> --allow <glob> [--allow ...] [--max-bytes 65536]` → exit 0 only if every file in the diff matches an allowed glob, no file matches `*.log`, `patch_*`, `test_*.js` at repo root, `package.json`, `package-lock.json`, `*.lock` or `requirements*.txt` unless explicitly allowed, and the patch is under the byte cap; on failure print one line per violation and exit 1. Pagination via nextPageToken everywhere.
5. ENV NOTE: no network in tests; if your VM blocks something, report it.
6. DELIVERABLE: one PR titled "tools/jules.py: API client with review gates (create/status/diff/report/gate) + tests"; description = the subcommands, files, pytest summary line.
7. No questions needed; proceed.
