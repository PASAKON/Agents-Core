1. GOAL: `pytest tests/test_worker_init_hook_inbox.py` (new) passes and `pytest` stays green. The test calls `runners.worker_init._write_dev_settings(worktree, role)` on a tmp_path worktree that is NOT an Agents checkout and asserts the written `.claude/settings.local.json` contains a `UserPromptSubmit` hook whose command runs the Agents repo's `scripts/hook-inbox.py` by absolute path (so a DEV in a foreign worktree drains its mailbox).
2. FILES you may touch: `tests/test_worker_init_hook_inbox.py` (new); `runners/worker_init.py` ONLY if the test fails on main as-is — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: editing scripts/hook-inbox.py or lib/mailbox.py.
4. KNOWN LEAD (GitHub issue #147, 2026-09-09): `_write_dev_settings` (around line 301) once wrote only a `Stop` hook, so a DEV whose worktree was a cookierun-bot checkout never received mailbox bodies ("header only"). Issue #154 was fixed on 2026-09-21 (task-c4dfb495) and may have fixed this too — your job is to PROVE the current state with the test: if it passes on main unchanged, the PR contains only the test and its description says "#147 is already fixed on main since <commit>" with the hash you find via `git log -S hook-inbox -- runners/worker_init.py`; if it fails, fix `_write_dev_settings` minimally.
5. ENV NOTE: no tmux/iTerm needed; report anything missing.
6. DELIVERABLE: one PR titled "test(worker_init): foreign worktrees get the hook-inbox UserPromptSubmit hook (#147)"; description = verdict (fixed since <commit> / fixed now), files, pytest summary line; reference #147.
7. No questions needed; proceed.
