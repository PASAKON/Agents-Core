Check whether GitHub issue #147 (a DEV working in a non-Agents worktree never gets its mailbox drained because the hook-inbox hook isn't written) is still a problem on main. Add a test that proves the current behaviour, fix it if needed, and open a PR.
