1. GOAL: `python3 -m pytest -q` passes from the repo root with new tests covering existing behaviour: `parse()` and `window()` in `server/contabo-usage/codex-feed.py`, and `parse_line()`, `newest()`, `window()` in `server/codex-usage/feed.py`. Today the repo has no tests at all.
2. FILES you may touch: `tests/test_codex_feed.py` (new), `tests/test_codex_usage_feed.py` (new), `tests/conftest.py` (new, only if needed to load files from hyphenated directories via importlib), `pytest.ini` (new, `testpaths = tests`) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: editing anything under `server/` or `widgets/`; dependencies beyond pytest; network or ssh calls in tests — functions that shell out are NOT to be exercised; test the pure functions with synthetic dicts and lines only.
4. KNOWN LEAD: the modules live in directories with hyphens, so load them with `importlib.util.spec_from_file_location`. Synthetic inputs: for codex-feed `parse()` use the JSON shape documented in its own docstring (`rateLimits.primary.usedPercent/windowDurationMins/resetsAt`, `secondary` null, `credits.balance` as a string); assert `window()` returns None when `windowDurationMins` is missing, that `resets_at` is ISO-8601 UTC, and that a non-numeric balance yields None. For codex-usage `newest()`, feed several parsed lines with different `reset_at` values and assert it picks the max; assert `parse_line()` returns None on a non-JSON line.
5. ENV NOTE: only Python 3.12 + pytest are needed; report anything else missing.
6. DELIVERABLE: one PR titled "tests: first pytest suite — codex feed parse/window/newest (pure functions only)"; description = files, what each test pins, the pytest summary line.
7. No questions needed; proceed.
