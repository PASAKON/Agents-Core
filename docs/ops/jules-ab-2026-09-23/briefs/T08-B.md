Write a small Python CLI, tools/jules.py, for the Jules API (create a session, check status, dump the diff, and a `gate` command that checks a diff only touches allowed files and isn't too big). Add tests. Open a PR.
