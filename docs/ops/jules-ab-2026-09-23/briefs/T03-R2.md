1. GOAL: a new test `scripts/test_close_dev_releases_locks.py` reproduces GitHub issue #159 and passes after your fix; plain `pytest` stays green.
2. FILES you may touch: the new test file, plus whichever ONE or TWO source files the reproduction shows are responsible (name them in the PR). No other file.
3. FORBIDDEN: scratch/log/patch files; dependency or lockfile edits; editing or loosening existing tests; unrelated refactors.
4. WHAT IS KNOWN (verified 2026-09-23): the test must FIRST fail on main. Reproduce with the repo's sqlite DB in a tmp dir (monkeypatch `lib.db.DB_PATH`, then `lib.db.init()`): insert a task with host='winbox', status in_progress, and two rows in the `locks` table owned by it; then close it the way the CTO does — the MCP tool `close_dev` in `runners/cto_mcp_server.py` (which calls `tools.worker_reap.close_dev`) followed by whatever sets it `done`; assert no lock row remains. `lib/db.py` has `RELEASING_STATUSES` (includes "done") and `update_status()` releases locks for those — so the leak is on a path that sets the status WITHOUT `update_status`, or closes without any status change. Find that path from the failing test; do not assume. `lib.db.release_task_locks(task_id, project)` exists and works. Measured incident: task-54ff705b (winbox) closed with close_dev(status="done"), row went to done, its 2 path locks stayed. If any fact in this brief turns out to be wrong, say which one in the PR description and continue with the correct fact — do not stop to ask.
5. ENV NOTE: pytest collects only `scripts/` and `lib/` (pytest.ini testpaths) — put new tests in `scripts/`, never `tests/` (not collected). Use `assert`; a test that returns True/False passes pytest either way. Your VM may lack packages CI installs (pyyaml, psycopg): report it in the PR, do not work around it.
6. DELIVERABLE: one PR titled "fix: release path locks on every close path (#159)"; description = the path that leaked, files, and the pytest summary line before and after the fix.
7. No questions needed; proceed.
