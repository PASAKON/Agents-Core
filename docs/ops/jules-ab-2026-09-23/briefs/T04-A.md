1. GOAL: a new test `tests/test_reopen_task_rewrites_taskmd.py` passes and `pytest` stays green: after `reopen_task(task_id, feedback)`, the task's worktree `TASK.md` contains the feedback text and an iteration marker, so a worker that re-reads TASK.md sees the new brief.
2. FILES you may touch: `runners/cto_mcp_server.py` (the `reopen_task` tool), one helper module ONLY if the rewrite logic belongs next to the TASK.md writer that `runners/worker_init.py` already uses (name it in the PR), and the new test — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: touching the mailbox/wake path or the watchdog (parts 2 and 3 of the issue, out of scope).
4. KNOWN LEAD (GitHub issue #158 part 1, 2026-09-22): `reopen_task` records the feedback in the database but the worker's `TASK.md` still holds the iteration-0 brief — `grep -c "<feedback phrase>" <worktree>/TASK.md` returned 0 after a reopen. Look at how `runners/worker_init.py` writes TASK.md on spawn and reuse that writer; append an "## Iteration N feedback (from CTO)" section rather than replacing the original brief. Use the repo's sqlite test fixtures and a tmp_path worktree in the test.
5. ENV NOTE: no tmux/iTerm on your VM is expected; mock or skip exactly as neighbouring tests do; report anything else missing.
6. DELIVERABLE: one PR titled "fix(reopen_task): rewrite the worker's TASK.md with the feedback (#158 part 1)"; description = root cause, files, pytest summary line; reference #158.
7. No questions needed; proceed.
