1. GOAL: `python3 tools/check_links.py` (new) exits 0 when every relative markdown link `[text](path.md)` / `[text](path.md#anchor)` and every `[[wikilink]]` in the repo resolves to an existing file, and exits 1 printing one line per broken link as `<file>:<line> -> <target>`. Run it on the current repo and paste its full output (count + list) into the PR description. Do NOT fix any link.
2. FILES you may touch: `tools/check_links.py` (new), `README.md` (add one usage line under an existing heading) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: editing any other .md file even if a link is broken; dependencies beyond the standard library.
4. SPEC: relative links resolve from the linking file's directory; ignore `http://`, `https://`, `mailto:` and bare `#anchor` links; a `[[name]]` resolves if some `name.md` exists anywhere in the repo (case-sensitive; strip an optional `|alias` suffix); skip fenced code blocks; the exit code is the verdict.
5. ENV NOTE: Python 3 only; report anything missing.
6. DELIVERABLE: one PR titled "tools/check_links.py: markdown + wikilink checker (report only)"; description = usage, the count of broken links found and the list.
7. No questions needed; proceed.
