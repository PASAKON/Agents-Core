1. GOAL: `pytest tests/test_hook_browser_guard.py` (new) passes and `pytest` stays green. The test drives `scripts/hook-browser-guard.py` as a subprocess (stdin = synthetic PreToolUse JSON like the example in the script's own docstring, a tmp_path counter dir via whatever env/path the script uses) and asserts: (a) with the environment marking a DEV session the hook counts calls and screenshots instead of exempting them, (b) at call 61 / screenshot 16 it prints a warning to stderr and still allows the call (exit 0, no deny), never blocks, (c) C-level sessions keep today's caps of 12 calls / 5 screenshots and today's behaviour.
2. FILES you may touch: `scripts/hook-browser-guard.py`, `tests/test_hook_browser_guard.py` (new) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: changing the C-level caps or the counter file format; launching any browser.
4. KNOWN LEAD (CEO to-do 2026-08): the hook exempts DEV sessions entirely (the to-do says the check is on `DEV_ROLE` — confirm in the file; if the exemption is already gone, say so in the PR and only add the test). browser_operator DEVs are the sessions that screenshot the most, and their "40 steps / 5 screenshots" ceiling is only prose in a skill, so a looping operator can take 200 screenshots with nothing stopping it. Wanted: DEVs are counted with higher caps — 60 calls / 15 screenshots — and get a WARNING (stderr message, call allowed) rather than a block, so real work never dies from the guard.
5. ENV NOTE: the hook is a pure stdin→stdout/stderr script; nothing to install. Report anything missing.
6. DELIVERABLE: one PR titled "hook-browser-guard: count DEV sessions (60 calls / 15 shots, warn not block)"; description = root cause, files, pytest summary line.
7. No questions needed; proceed.
