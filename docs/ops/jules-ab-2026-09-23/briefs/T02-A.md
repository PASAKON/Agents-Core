1. GOAL: osv-scanner (`osv-scanner --recursive ./`, as run by .github/workflows/ci.yml) reports 0 vulnerabilities for requirements.txt, and `pytest` stays green.
2. FILES you may touch: `requirements.txt` only — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: touching any other dependency line.
4. KNOWN LEAD: osv-scanner on 2026-09-22 flagged `anyio 4.9.0` (the lowest version our floor `anyio>=4.0` permits): GHSA-82r6-8w77-94w6 (CVSS 9.3) and GHSA-5p39-cfhj-2xmp (CVSS 6.8), both fixed in 4.14.2. Raise the floor to `anyio>=4.14.2` and add a one-line comment in the style of the existing "Security floors" block (ADR 0020 osv_check) naming the two advisories and the date.
5. ENV NOTE: if osv-scanner is not on your VM, do not install or emulate it — state in the PR that CI runs it.
6. DELIVERABLE: one PR titled "deps: anyio>=4.14.2 — GHSA-82r6-8w77-94w6 + GHSA-5p39-cfhj-2xmp (osv 2026-09-22)"; description = the two advisories, the file changed, the pytest summary line.
7. No questions needed; proceed.
