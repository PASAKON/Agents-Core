The browser-guard hook (scripts/hook-browser-guard.py) exempts DEV sessions completely, so a browser_operator can loop and take hundreds of screenshots. Make it count DEVs too with a higher ceiling and warn instead of block. Add a test and open a PR.
