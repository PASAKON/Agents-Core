1. GOAL: `pytest tests/test_delegate_missing_worktree.py` (new) passes and `pytest` stays green: `tools.delegate.delegate_task` on a task row whose recorded worktree path no longer exists on disk returns an explicit refusal (status text naming the missing path and "merge_task cleaned it — create a new task or recreate the worktree") and never reports success with `pid=0`. Only the refuse-loudly half — do NOT implement recreating the worktree.
2. FILES you may touch: `tools/delegate.py`, `tests/test_delegate_missing_worktree.py` (new) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: touching tmux/iTerm/ssh spawn code paths beyond the early check; changing `merge_task` or the watchdog; the test must use the repo's sqlite fixtures + tmp_path, never a real tasks.db.
4. KNOWN LEAD (measured 2026-09-19 18:31, task-6eabea66): `reopen_task` + `delegate_task` on a task whose worktree `merge_task` had just removed → the delegate log said "DEV spawned (fire-and-forget)" but pid=0, "wake skipped (no live session)", no tmux, no worktree, empty delegate_log, and the watchdog marked it failed within 30 s. `delegate_task` already has an early-exit block for "claim failed, task not found, worktree missing" (around line 518) — extend or fix it so a missing worktree is caught BEFORE any spawn and surfaces as a refusal in the return value and the task's delegate_log, with the reason.
5. ENV NOTE: no tmux/iTerm on your VM; the early check runs before them. Report anything missing.
6. DELIVERABLE: one PR titled "delegate: refuse loudly when the task's worktree no longer exists (no silent pid=0)"; description = root cause, files, pytest summary line.
7. No questions needed; proceed.
