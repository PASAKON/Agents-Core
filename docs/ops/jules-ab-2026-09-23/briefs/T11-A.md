1. GOAL: `pytest tests/test_send_to_worker_tab_resolver.py` (new) passes and `pytest` stays green. The test pins the id→tab resolution logic in `tools/send_to_worker.py` as pure string logic (never running osascript): two task ids that share the same first six characters (`task-a…` vs `task-a…`) must never resolve to each other's tab; matching is exact/unique, and an ambiguous match raises instead of picking one.
2. FILES you may touch: `tools/send_to_worker.py`, `tests/test_send_to_worker_tab_resolver.py` (new) — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: touching lib/mailbox.py or the wake path; anything that executes osascript in tests.
4. KNOWN LEAD (GitHub issue #65, CEO to-do): when the tab is not found by the full id, the code fell back to matching `full_id[:6]` — the first five characters are always `task-`, leaving one character of entropy, and the AppleScript check was `contains`, not equals. The CTO saw a message land in another live DEV's tab with a receipt saying it was delivered to the right one. Note: task-2f04a8ca (2026-08-14) moved delivery onto mailbox+wake, so the fallback may already be gone — the file's own header still mentions `full_id[:6]`. Your job is to PROVE the current state: if the substring fallback is gone, the PR contains only the test and its description says so with the commit you found (`git log -S "[:6]" -- tools/send_to_worker.py`); if it still exists, replace it with exact, unique matching.
5. ENV NOTE: macOS/osascript are absent on your VM by design; the resolver must be testable without them. Report anything else missing.
6. DELIVERABLE: one PR titled "send_to_worker: tab resolver matches the full id exactly, never a 1-char substring (#65)"; description = verdict (already fixed since <commit> / fixed now), files, pytest summary line; reference #65.
7. No questions needed; proceed.
