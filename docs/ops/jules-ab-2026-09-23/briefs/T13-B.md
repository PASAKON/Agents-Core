Bug: delegating a task whose worktree was already cleaned up by merge_task reports "DEV spawned" with pid=0 and then dies silently. Make delegate refuse with a clear message instead. Add a test, open a PR.
