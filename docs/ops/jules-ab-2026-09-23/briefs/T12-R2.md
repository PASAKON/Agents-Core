1. GOAL: plain `pytest` green AND `grep -rniE 'zai|ZAI_|ninerouter|NINEROUTER|9router|provider_overrides|quota_router|GLM_PREFIX|USE_GLM' lib scripts runners tools docs/ops/model-fallback.md` prints nothing. AND `python scripts/test_mcp_role_config.py` prints ALL PASS (it is a script-style test pytest cannot fail).
2. FILES — the complete list that references them today (verified by that grep on 2026-09-23): lib/config.py, lib/quota_router.py (delete), runners/cto.py, runners/cto_chat.py, runners/secretary_server.py, runners/worker_init.py, runners/worker_resume.py, scripts/cto-claude.sh, scripts/cxo-claude.sh, scripts/spawn-cto.sh, scripts/spawn-cxo.sh, scripts/install-secretary-waker.sh, scripts/ninerouter-up.sh (delete), scripts/test_quota_router.py (delete), scripts/test_secretary_server.py, scripts/test_mcp_role_config.py, tools/terminal_restart.py, docs/ops/model-fallback.md. No other file.
3. FORBIDDEN: .env or any secrets file; npm/uninstall/machine state (the CEO's, outside the repo); scratch/log files; changing the Claude path: the launchers must still pass exactly `--model "$MODEL" --fallback-model … --effort "$EFFORT"` resolved from policies/agents.yaml (C-levels resolve to claude-opus-5-5[1m] today — keep that).
4. WHAT IS KNOWN: CEO order 2026-09-20 — Z.ai is cancelled and 9Router retired; Codex/Antigravity run through their own tools. `cxo_provider_overrides` / `worker_provider_overrides` / `_provider_overrides` in lib/config.py exist only for those providers: remove them AND their call sites in runners/cto.py, cto_chat.py, worker_init.py, worker_resume.py (each call site keeps the plain Claude model it already computes). In the launchers remove the GLM block and `--glm`/GLM_PREFIX; keep ENV_PREFIX. Update the GLM_PREFIX regex in scripts/test_mcp_role_config.py to the new command string. Rewrite docs/ops/model-fallback.md in <= 40 lines: C-levels on Claude credit; Codex/Antigravity via briefs (IRON-RULES §53, skill delegate-external-agent); no proxy. If any fact in this brief turns out to be wrong, say which one in the PR description and continue with the correct fact — do not stop to ask.
5. ENV NOTE: pytest collects only `scripts/` and `lib/` (pytest.ini testpaths) — put new tests in `scripts/`, never `tests/` (not collected). Use `assert`; a test that returns True/False passes pytest either way. Your VM may lack packages CI installs (pyyaml, psycopg): report it in the PR, do not work around it. Also run `bash -n` on every edited .sh file.
6. DELIVERABLE: one PR titled "remove Z.ai + 9Router (CEO 2026-09-20): config, call sites, launchers, docs"; description = the empty grep output, deleted vs edited files, pytest summary line, and the test_mcp_role_config.py result line.
7. No questions needed; proceed.
