1. GOAL: `pytest` green AND `grep -rniE 'zai|ZAI_|ninerouter|NINEROUTER|9router' lib scripts docs .claude` returns nothing. Both are the verdict.
2. FILES you may touch (the complete list of files that reference them today): `lib/config.py`, `lib/quota_router.py` (delete), `scripts/test_quota_router.py` (delete), `scripts/ninerouter-up.sh` (delete), `scripts/cto-claude.sh`, `scripts/cxo-claude.sh`, `scripts/spawn-cto.sh`, `scripts/spawn-cxo.sh`, `scripts/install-secretary-waker.sh`, `scripts/test_secretary_server.py`, `docs/ops/model-fallback.md` — no other file.
3. FORBIDDEN: scratch, log or patch files in the repo; dependency or lockfile edits unless listed in line 2; editing or loosening any existing test unless the task IS tests; unrelated refactors. Also forbidden here: touching `.env` or any secrets file; `npm uninstall`, removing `~/.ninerouter`, or any machine state (those are the CEO's, outside the repo); changing behaviour for the remaining provider (Anthropic/Claude): `worker_provider_overrides` and `cxo_provider_overrides` are called on EVERY worker spawn — after your change they must still return a valid mapping for the default provider, with the existing tests proving it.
4. KNOWN LEAD (CEO order 2026-09-20): Z.ai is cancelled and 9Router is retired — the org routes Codex/Antigravity through their own tools instead of a proxy. In `lib/config.py` remove the zai + ninerouter entries from `_PROVIDER_ENDPOINTS`, `_PROVIDER_KEY_VAR`, `_PROVIDER_DEFAULT_MODEL` and simplify `_provider_overrides` / `worker_provider_overrides` / `cxo_provider_overrides` accordingly (delete them only if nothing else calls them — check with grep and say what you found). `lib/quota_router.py` exists only to compare Claude vs Z.ai quota → delete it with its test. In the four launcher scripts cut the provider export/check blocks. Rewrite `docs/ops/model-fallback.md` in ≤ 40 lines: the lanes are now Claude credit for C-levels and Codex/Antigravity via briefs (see IRON-RULES §53 and skill delegate-external-agent) — no proxy.
5. ENV NOTE: shell scripts are not executed in CI; keep them syntactically valid (`bash -n`). Report anything missing.
6. DELIVERABLE: one PR titled "remove Z.ai + 9Router (CEO 2026-09-20): config, quota_router, launcher scripts, model-fallback doc"; description = the grep proving zero references, the list of deleted vs edited files, the pytest summary line, and any caller of the removed functions you found.
7. No questions needed; proceed.
