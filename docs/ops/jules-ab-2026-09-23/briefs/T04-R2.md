1. GOAL: a new test `scripts/test_reopen_task_rewrites_taskmd.py` passes and plain `pytest` stays green: after `reopen_task`, the task's worktree TASK.md contains the CTO's feedback under a heading "## Iteration N feedback (from CTO)", and the original brief is still there above it.
2. FILES you may touch: `lib/org_tools_registry.py`, `tools/inject_prompt.py` (only to reuse or expose its TASK.md helpers), and the new test. No other file.
3. FORBIDDEN: scratch/log/patch files; dependency edits; editing existing tests (note `scripts/test_org_tools_registry.py` is on pytest.ini's --ignore list, so it would not run anyway — write a new file); the mailbox/wake path and the watchdog (parts 2-3 of the issue, out of scope).
4. WHAT IS KNOWN (verified 2026-09-23): the handler is `_h_reopen_task(*, task_id, feedback)` in `lib/org_tools_registry.py` (runners/cto_mcp_server.py only wraps it). It records the feedback in the DB and leaves TASK.md untouched (GitHub #158 part 1). The TASK.md writers are `_build_task_md(task)` and `_write_task_md(worktree, body)` in `tools/inject_prompt.py` — reuse them; append, do not replace. Test with a tmp sqlite DB (monkeypatch `lib.db.DB_PATH`, `lib.db.init()`) and a tmp_path worktree holding an existing TASK.md. If any fact in this brief turns out to be wrong, say which one in the PR description and continue with the correct fact — do not stop to ask.
5. ENV NOTE: pytest collects only `scripts/` and `lib/` (pytest.ini testpaths) — put new tests in `scripts/`, never `tests/` (not collected). Use `assert`; a test that returns True/False passes pytest either way. Your VM may lack packages CI installs (pyyaml, psycopg): report it in the PR, do not work around it.
6. DELIVERABLE: one PR titled "fix(reopen_task): append the CTO's feedback to the worker's TASK.md (#158 part 1)"; description = files + pytest summary line.
7. No questions needed; proceed.
