# HARVEST — เก็บคลิปที่ CEO generate วันนี้ (11 ก.ย. 2026) ลง Fix-2

**This file is the source of truth. Ignore any partial pane message that disagrees with it.**

Written by CTO 2026-09-11 14:30 Thai. Machine: **winbox** (`MACHINE-LOCK.txt` = FREE, take it).

---

## ⛔ THIS IS A HARVEST TASK. YOU GENERATE NOTHING.

**Zero Generate clicks. Zero Rerun clicks. Zero Recreate clicks.** You are collecting
clips that already exist. If you find yourself about to click anything that could fire a
generation, you have misread this brief — stop and report.

The only buttons you touch: navigation, the **Download** control on a finished card, and
**Confirm Rights** if a card demands it before downloading (standing CEO approval — see
`higgsfield-unlimited-gen` hard rule 3; that approval covers the rights banner only, nothing
else).

---

## The situation

The CEO generated clips on Higgsfield himself this morning — roughly **07:18 Thai onwards**
(he was testing Seedance **Video Edit** and **Image Edit** on the plaque/price-tag shots, and
firing scene prompts). **None of it has been downloaded or filed.** Everything before 07:18
Thai was already harvested by the overnight shift and is on Drive.

Your job is to find what is not filed yet, download it, and report what you found.

## Project URL — read the address bar, it matters

```
https://higgsfield.ai/generate/@ilag-studio/ai-film-festival-3
```

That is «Sorry, Sir». The other film (`ai-film-festival`, no `-3`) is a different project and
its clips are NOT ours. **Also check the account-level generations feed**, not only this
project's History — Video Edit and Image Edit outputs do not always land in the project view.

## The cut-off

Anything created **on or after 2026-09-11 00:00 Thai time** is in scope. In practice
everything from **07:18 Thai** onward is unfiled; the few between 00:00 and 07:18 are already
on Drive and are listed below so you can skip them.

## Already on Drive — DO NOT re-download these

These are the clips filed into `Fix-2` in the last 24 h. If a card on Higgsfield corresponds
to one of these, skip it.

```
S2PT-TourTogether-Fix1-take4      S2AC-Interpretations-Fix1-take1
S2AC-...take2-FLAGGED-tableau     S2AC-...take3-FLAGGED-corridor-not-wall
S2AC-Interpretations-Fix1-take4   S17b-TheHoleFromInside-Fix1
S2AC-...take5-FLAGGED-duplicate-woman
S2AC-...take6-FLAGGED-plaque-too-high
S2AC-Interpretations-Fix1-take7-v5   ← newest filed, 07:18 Thai. Everything after it is yours.
```

Full list of all 52 filed names is in the pane message if you need it; the nine above are the
only ones inside today's window.

---

## What to do, in order

1. **Take the machine.** Write your task id into `C:\mooniex\MACHINE-LOCK.txt`. Clear it when
   you finish. A Resolve/grading session may want this box later today.
2. Open the project URL. **Maximise the window and verify `window.innerWidth >= 1280`** before
   anything else — Higgsfield silently serves a mobile layout below that and half the controls
   vanish (`higgsfield-unlimited-gen`, HARD rule).
3. Walk the History / generations feed from newest backwards until you pass
   **2026-09-11 00:00 Thai**. Stop there.
4. For **every video card in that window that is not in the skip list above**, record before
   you download (ids cannot be recovered, files always can — §9):
   - asset id
   - created time as the UI shows it
   - duration and resolution
   - **the first ~150 characters of its prompt** (open the card's Info/Prompt panel) — this is
     how I map it to a scene, and without it the clip is unusable to me
   - whether it is a plain generation or a **Video Edit / Image Edit** output
5. **Download each one** to `C:\mooniex\harvest-20260911\`. Keep whatever filename Higgsfield
   gives it — do not invent scene names, you do not have the information to name them and a
   wrong name is worse than a raw one.
6. Also note **image** generations in the window (the CEO ran Image Edit on a wall plate).
   Report them with their prompts. Download them too, same folder.
7. `md5sum` every downloaded file.

## What to report back

A table, one row per item: asset id · time · type (video/image/edit) · duration · filename ·
size · md5 · the prompt excerpt. Plus anything that would not download and why.

**Do not upload anything to Google Drive.** I file it from my side once I have mapped each clip
to its scene. You have no filing rules and Drive is out of your scope.

## If something blocks you

Report anyway and say what stopped you. A report that says "I could not do X because Y" is
worth far more than silence — I cannot see your screen.

## Screenshot discipline

Read state as text where text exists (asset ids, prompts and timestamps are all text in the
DOM). Screenshot to decide, not to narrate. Every image you take is re-sent on every later
turn, so a long session gets expensive quadratically, not linearly.
