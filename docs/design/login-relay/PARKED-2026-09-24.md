**PARKED 2026-09-24 ~02:45 +07 by the CEO** because the Mac disk and RAM are in crisis (3.8 GB free, 16 MB RAM free, load 13-15). Resume only when the Mac has headroom.

## Symptom (live, CEO on iPhone, Mac:9226 Google sign-in, direct-touch mode)
The CEO can tap but can't press buttons or type reliably.

## Evidence (coordinate-only tap logger injected into the remote page, 02:38 +07)
- **Taps do reach the remote page.** 13 mousedown/click pairs were logged.
- **Vertical is right.** y followed the finger (187-347), and one tap landed on `#identifierId`, which then held focus.
- **Horizontal is broken.** **x = 260 on every tap**, whether the CEO aimed at the email field (x≈195) or Next (x≈326).
- Remote viewport during the session: 390x844, dpr 3, iPhone UA, so the emulation is correct.

## Not known yet
Why x is constant. Candidates, none tested:
- clientX from `changedTouches` on iOS combined with the letterbox maths in `handlePointerTap`;
- the screencast `<img>` width/height relation on the phone;
- the phone-side layout of `#screencast-img`.

## Fixed and deployed this session (Mac + Contabo, all on origin)
- `0f32717`: focus the hidden input on touchend (iOS keyboard).
- `c643f0b`: map taps with the frame's CSS-px size (was 3x off).
- `1bb75fc`: letterbox-aware mapping; the same operator takes the relay slot over (was "Disconnected: peer closed").
- `d7f0bf6`: CDP probe 1.5 s, peer targets 6 s (was "no Chrome found" on a loaded Mac).
- `41f9ee6`: mac-console.sh derives CONSOLE_DIR (Console-Mac had crash-looped after the HQ move).

## Also open
- The relay lists every automation Chrome. The CEO opened another CTO's ChatGPT Chrome (9223) by mistake, and it was verified harmless. Non-login pages need filtering or a warning.
- Error texts mislead: "peer closed" meant "in use", and "no Chrome found" meant "probe timed out".
- The CEO asked for the input box back (type into the tapped field) alongside direct touch.
- The vault (Face ID + PRF) has never been exercised on a real iPhone.

## How to resume (don't ask the CEO to test first)
1. Reproduce on the Mac with a touch-emulated phone viewport, a real touchend, and the real `#screencast-img` layout. Prove x varies and lands (tap logger on the remote page) before any CEO test.
2. Or run a browser_operator when the browser cap has a free slot.
3. Fix x, then restore the input box, then filter non-login Chromes.
