# Login relay — design backups (2026-09-23/24, CTO b4ed592c)

Backups of the design material made while building the phone login relay (MoonieX-Console `/relay`).
The live Artifacts are the primary copies. These files outlive the session scratchpad.

| file | what |
|---|---|
| `ask-inbox.html` | Ask Inbox design (logins + `!` from one phone). Artifact https://claude.ai/artifact/Hx4wcNjYctLx267dNuerAW |
| `relay-mockup.html` | relay flow mockup (auto QR/form, wait → result + Error ID). Artifact https://claude.ai/artifact/UX5S39UJKGUmJH873JfZmB |
| `vault-mockup.html` | password vault mockup (save after login, Face ID autofill). Artifact https://claude.ai/artifact/XZymGXxPDPNuREGMkJWqik |
| `PARKED-2026-09-24.md` | why it is parked, the evidence, and how to resume (same text as MoonieX-Console#9) |
| `operator-tap-test-brief.md` | the browser_operator brief used for the end-to-end tap test |
| `lungnote_todo_grep.py` | search open LungNote todos by text (PostgREST `ilike`). The MCP `list_todos` can't filter |

Status: **PARKED** — see MoonieX-Console#9 and LungNote bf618101.
