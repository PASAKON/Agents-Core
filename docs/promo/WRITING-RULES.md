# กฎเหล็กการเขียนข้อความที่จะโพสต์ออนไลน์

CEO สั่ง 2026-09-17 · บังคับใช้กับ **ทุกข้อความที่เผยแพร่ในนาม CEO หรือ ILAG**
โพสต์ · คอมเมนต์ · แคปชั่น · ข้อความทักลูกค้า · bio · คำอธิบายวิดีโอ

> *"ฉันเป็นตัวแทนของประโยคนั้น ก็อยากจะให้มันออกมาในแบบที่ฉันอยากให้เป็น
> ข้อความที่เราโพสเป็นข้อความที่ฉัน Verify"* — CEO

---

# กฎข้อ 1 (HARD) · ห้ามใช้ em dash เด็ดขาด

**ห้ามใช้ `—` ในข้อความที่จะโพสต์ ทุกกรณี ไม่มีข้อยกเว้น**

**Why hard:** มันคือลายเซ็นของข้อความที่เขียนด้วย AI ที่คนจับได้ง่ายที่สุด
คนไทยพิมพ์ออนไลน์**แทบไม่เคยใช้** เพราะแป้นพิมพ์ไทยไม่มีปุ่มนี้ ต้องตั้งใจแทรกเท่านั้น
โพสต์ที่มี `—` = โพสต์ที่อ่านแล้วรู้ว่าไม่ใช่คนพิมพ์ ซึ่งทำลายจุดยืนทั้งหมดที่เราวางไว้
ว่าจะโปรโมทในนามคนจริง

## ใช้อะไรแทน

| แทนที่จะเขียน | เขียนแบบนี้ |
|---|---|
| `รอยร้าว — ยาวเท่าฝ่ามือ` | `รอยร้าว ยาวเท่าฝ่ามือ` (เว้นวรรคเฉย ๆ) |
| `ผมลอง 6 เทค — ผ่านแค่เทคเดียว` | `ผมลอง 6 เทค ผ่านแค่เทคเดียว` |
| `ผมลอง 6 เทค — ผ่านแค่เทคเดียว` | `ผมลอง 6 เทค ผ่านแค่เทคเดียว` หรือ **ขึ้นบรรทัดใหม่** |
| `มันพัง — แล้วผมก็รู้ว่าทำไม` | `มันพัง แล้วผมก็รู้ว่าทำไม` |

**วิธีที่คนไทยใช้จริง เรียงตามความถี่:**
1. **เว้นวรรค** เฉย ๆ
2. **ขึ้นบรรทัดใหม่**
3. `...` (จุดสามจุด พิมพ์เอง ไม่ใช่ตัว `…`)
4. `(วงเล็บ)`

---

# กฎข้อ 2 (HARD) · ตัวอักษรอื่นที่ห้ามใช้ด้วย

ทั้งหมดนี้คนไทยไม่ได้พิมพ์กันบนมือถือ เพราะ**ไม่มีในแป้นพิมพ์**

| ห้าม | ชื่อ | ใช้แทน |
|---|---|---|
| `—` | em dash | เว้นวรรค / ขึ้นบรรทัด |
| `–` | en dash | `-` |
| `…` | ellipsis | `...` |
| `“ ” ‘ ’` | smart quotes | `" "` `' '` |
| `·` | middle dot | `-` หรือ `/` |
| `→ ←` | ลูกศร | `->` หรือเขียนเป็นคำ |
| `×` | multiplication sign | `x` |

**Why hard:** เหตุผลเดียวกับข้อ 1 ทุกตัวต้องตั้งใจแทรก พิมพ์ปกติไม่ได้
เจอตัวเดียวก็พอให้คนสงสัยแล้ว

---

# กฎข้อ 3 · เขียนให้เหมือนพิมพ์ในมือถือ

สิ่งพวกนี้ไม่ใช่กฎเหล็ก แต่เป็นสิ่งที่ทำให้อ่านแล้วเป็นคน

- **ประโยคสั้น** คนไทยพิมพ์ในมือถือ ไม่เขียนเรียงความ
- **ขึ้นบรรทัดบ่อย** ไม่ใช่ย่อหน้ายาว
- **ไม่ต้องสมมาตร** ไม่ต้องทุกข้อยาวเท่ากัน ไม่ต้องขนานกันทุกบรรทัด
- **ตัวหนาน้อย ๆ** ถ้าหนาทุกประโยค = ไม่มีประโยคไหนสำคัญ
- **พิมพ์ผิดนิดหน่อยไม่เป็นไร** สมบูรณ์แบบเกินไปคือสัญญาณเตือนอีกอย่าง
- **อีโมจิใช้ได้** แต่แบบที่คนใช้จริง ไม่ใช่ใส่หัวข้อทุกบรรทัดแบบสไลด์นำเสนอ

---

# วิธีบังคับ ไม่ใช่แค่จำ

**มีสคริปต์ตรวจให้แล้ว** อยู่ที่ `scripts/check-post-text.sh`

```bash
./scripts/check-post-text.sh docs/promo/POST-PLAN-W1.md
./scripts/check-post-text.sh -   # อ่านจาก stdin
```

- เจอตัวต้องห้าม = **exit 1** พร้อมบอกบรรทัดและตัวที่เจอ
- ไม่เจอ = exit 0

**กติกา: ทุกร่างที่จะส่งให้ CEO ตรวจ ต้องผ่านสคริปต์นี้ก่อน**

> เขียนกฎไว้เฉย ๆ แล้วหวังว่าจะจำได้ คือวิธีที่พลาดมาแล้วในโปรเจกต์นี้หลายครั้ง
> กฎที่ดีคือกฎที่เครื่องปฏิเสธให้เราเอง

---

## ขอบเขต

**บังคับ:** ทุกอย่างที่เผยแพร่ในนาม CEO หรือ ILAG
โพสต์ · คอมเมนต์ · แคปชั่น · ข้อความทัก · bio · คำอธิบายวิดีโอ · อีเมลถึงคนนอก

**ไม่บังคับ:** เอกสารภายใน รายงานที่คุยกันในแชท โค้ด คอมเมนต์ในโค้ด
(ถ้า CEO อยากให้ครอบคลุมแชทด้วย บอกได้ เปลี่ยนได้ทันที)
