# task-9a33b2a3 — Flow on winbox, progress log

- Built `tools/flow_cdp.py`: single `--cdp-url` > `$FLOW_CDP` > `http://127.0.0.1:9226` resolver, `enforce_platform()` refuses darwin (exit 2) unless `FLOW_ALLOW_MAC=1`. Wired into `flow_shoot.py`, `flow_upload_element.py`, `flow_music.py`, `flow_reupscale.py` (via `FlowBrowser`); each real `attach()` calls `enforce_platform()` first.
- `flow_shoot.py` log path: now `<ledger dir>/flow_shoot.log` by default, `--log` still overrides. `cmd_run`/`cmd_pull` resolve `cdp_url` via `flow_cdp.pick_cdp_url(args.cdp_url)`.
- `tests/test_flow_cdp.py`: flag>env>default, darwin refusal, `FLOW_ALLOW_MAC` override (parametrized to confirm only the literal string `"1"` triggers it), plus real-`os.environ`/`sys.platform` sanity checks.
- Full test suite run in background (`bwntyj2a1`), deselecting two pre-existing/unrelated failures (`scripts/test_sompong_photo_filer.py` flaky real-socket test, `tests/test_machine_doctor.py` darwin-vs-linux assumption) — exit code 0, all dots, no failures.
- `windows/flow-chrome-debug.cmd`: launches Chrome on winbox with `--user-data-dir=C:\Users\passg\.flow-automation\chrome-profile --remote-debugging-port=9226 --no-first-run --no-default-browser-check --disable-notifications`, start URL Flow home.
- Verify step, pre-login, dry runs only:
  - `flow_shoot.py status` against a copy of `state/banchi/ACT7.tsv` (20 shots) — printed a status summary, touched no browser. OK.
  - `scripts/flow/winbox_pull.sh` against a real dummy file on winbox — first pass pulled it, second pass correctly skipped it as identical. Found + fixed a real bug live: macOS `scp` (SFTP-protocol default) fails on a backslash Windows remote path even though the file exists (`ssh winbox "type ..."` confirms it); fixed by normalizing the remote path to forward slashes before the `scp` call.
- winbox-pc-lease taken (`developer task-9a33b2a3: register+launch Flow Chrome debug profile on winbox:9226`, 30 min) → scp'd `flow-chrome-debug.cmd` + a new `windows/flow-chrome-task-register.ps1` (same `Register-ScheduledTask -LogonType Interactive` pattern as `windows/spawn-worker.ps1`/`windows/s1probe.ps1`) → registered + started `MooniexFlowChrome9226` as `GoB\passg` → verified `http://127.0.0.1:9226/json/version` responds (Chrome/154) → gave the lease back immediately (Cookie Run was already idle, nothing to restart).
- Checked winbox for a runnable checkout: `C:\mooniex\Agents\Core` is a sparse (`config, lib, scripts, state, tools`) checkout of `PASAKON/Agents-Core`, on `main`, not another worker's tree. `playwright` 1.47.0 already installed in `C:\mooniex\pwvenv` — no install needed.
- **Blocked**: getting my branch's tool changes onto that winbox checkout. TASK.md step 6 says "push your agent branch, fetch it on winbox", but the Hard Rules forbid `git push`; the fallback (scp the 5 changed `tools/*.py` files directly onto the shared checkout) was denied by the auto-mode classifier as a remote-shell-write risk. Sent a `dev_message` to the CTO flagging the conflict and asking for a decision. Everything else (Chrome/CDP, lease, pull script, tests, docs) is done and verified independently of this.
- Wrote `docs/ops/flow-on-winbox.md` — exact commands to start Chrome on winbox, run a shoot, and pull clips to the Mac.
- Login + zero-credit verify: not started — waiting on the CTO/CEO to confirm the Google login is live in the winbox Chrome profile per `relay-login` skill §0. Will not poll the tab.
