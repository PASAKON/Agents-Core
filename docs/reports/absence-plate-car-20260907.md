# Absence prop plate — retro-futuristic sports car — 2026-09-07

Task: task-bc7f8d5d. Goal: ONE 16:9 still image, prop Element candidate for
"the woman in green"'s car (retro-futuristic sports coupé). ZERO paid actions.
No Element created/registered — CEO approves the look first.

- **Model**: Kling O1 (image), project `.../ai-film-festival-3`. GPT Image 2
  was tried first — despite an "UNLIMITED" badge in the model picker, its
  Generate button zoomed to a live price (`8.5→6.5` credits, not zero), so it
  was **not clicked**, per the STOP rule. Kling O1 has its own "Unlimited"
  toggle in the composer (separate from the model-list badge); switching it
  ON made the Generate button read bare `UNLIMITED`, no digits.
- **Price zoom reading**: `UNLIMITED` (zero digits), confirmed by zoomed
  screenshot immediately before the single click.
- **Credits before / after**: 513/6,000 both times — unchanged. Account's
  "Free generations in total" counter went 664 → 665, confirming the free/
  Unlimited path was used, not credits.
- **Settings**: Kling O1, 16:9, count 1/4 (batch=1), prompt pasted via
  synthetic ClipboardEvent (744 chars, exact match, verified first/last 80
  chars) + End→space→Backspace bind tap. Window stayed at the shared 1024×591
  (never resized, per the other-worker-in-same-Chrome rule) — desktop
  composer rendered fine at that width regardless (full sidebar/Generate
  button present, no mobile lockup).
- **Result**: one PNG, 1360×768 (≈16:9), downloaded and saved to
  `docs/prompts/absence/generated/project_absence_prop_car_take1.png`. Shows
  a glossy emerald-green low-slung coupé with teardrop canopy, chrome centre
  spine, tail fins and round chrome lamps, three views on a warm-grey studio
  backdrop — no people, no logos, no text.
- **One odd thing**: the composed view order came out center=rear view /
  left=side profile / right=another rear-quarter view, not the requested
  front-three-quarter-center / side-left / rear-three-quarter-right layout.
  Content and finish otherwise match the prompt closely; flagging the view
  order as a minor miss for the CEO's look review, not re-firing without
  direction (one fire per task).
- No rights-verification banner, no NSFW/rejection flag on the card.

Fired once, per the ONE-click rule. Not made into an Element.
