claude-agent-sdk>=0.0.20
anyio>=4.14.2
pyyaml>=6.0
textual>=0.80
rich>=13.0
yfinance>=1.5.2
pandas>=3.0.5
# Runtime HTTP dep for scripts/supabase_storage_quota_check.py + fusion_query.py.
# Pinned now that pytest collects their mocked unit tests (ADR 0021); previously
# installed ad hoc only in shells that ran those scripts.
requests>=2.31,<3
# Test runner — ADR 0021 standardises on pytest (added 2026-08-13).
pytest>=8,<9

# Postgres backend for lib/db.py, selected by ORG_DB_URL (task-02ecbdea,
# docs/design/tasks-db-hub.md §3.1). Binary wheel — no local libpq/build
# toolchain needed. Importing lib.db_pg is always safe without this
# installed; only lib.db_pg.connect() (i.e. ORG_DB_URL actually set) needs it.
psycopg[binary]>=3.1,<4

# Security floors on transitive dependencies (ADR 0020 osv_check, 2026-08-13).
# We do not import these directly — idna arrives via requests/httpx/anyio and
# python-multipart via mcp. osv-scanner resolves the LOWEST version our
# constraints permit, and that floor carried 9 known CVEs (worst CVSS 8.7).
# Raising the floor is not an upgrade of anything we run: idna was already at
# 3.15 locally. python-multipart was at 0.0.29, still short of the 0.0.30/0.0.31
# three of those advisories require. Remove these lines once the upstream
# packages raise their own floors past these versions.
idna>=3.15
python-multipart>=0.0.31

mcp<2
