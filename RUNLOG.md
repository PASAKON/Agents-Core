- 2026-09-25T00:00:00Z start: task-fcd2b95d, seg04 of BL EP57 split-editor A/B (Arm 2). Brief printed via bl_ab_run.py spawn-seg. Window [104.5333, 153.0333)s.
- 2026-09-25T16:05:00Z read SCRIPT.tsv/timings.tsv/segments.json/PLAN.md/BRIEF-seg.md; confirmed my window covers CURIOSITY-3..5, SUMMARY-1..9 (12 lines, all shot-basename empty).
- 2026-09-25T16:07:00Z checked avatar windows (lip_a [0,14.9) lip_b [68.3,82.95) lip_c [137.16,152.51)) against my 12 lines' t0: only SUMMARY-7/8/9 (141.48/146.08/150.8) fall inside lip_c -- FF; the other 9 are outside every window -- KIN (matches the prior Arm1 pilot's independently-reached same conclusion, commit 0034f4d7, reviewed only after deriving my own).
- 2026-09-25T16:08:00Z reviewed media/broll/S29-S37.mp4 thumbnails (contact sheet) -- generic brand b-roll (RICH-beanie host, neon corridor, magnifying glass, files/keys, skyline), safe as darkened KIN default plates, no baked-in text.
- 2026-09-25T16:09:00Z copied decisions.base.jsonl, ran jev_edit.py final --tsv for all 55 question rows across my 12 lines (32 agree / 11 disagree / 0 jev_wrong_at_gate), filtered decisions.jsonl down to my window's lines only.
- 2026-09-25T16:10:02Z start: wrote beats.json (9 KIN + 3 FF), dry-run compose (--no-render) succeeded, no ComposeError (avatar-window check passed for the 3 FF beats).
- 2026-09-25T18:30:00Z first render finished (18m 18k mp4, 1080x1920/30fps/48.5s, frame count matches). QC by eye at 10 timestamps: EVERY caption()/kinetic() text overlay is missing (only video/img plates + the standing legal-disclaimer band show) across all 9 KIN beats and all 3 FF captions. Render log shows WARN sub_timeline_readiness_timeout + "BeginFrame did not run...Heavy compositions can stall on software GL". Box load average 5.86 on this 4-core Contabo VPS (other segment editors' sessions active concurrently, task-94eb699b/seg02 doing its own trim_range ffmpeg right after mine finished). Root cause read from hyperframes cli.js: any script/measurement failure in the composed page's GSAP timeline setup (tl) at capture time silently drops ALL tl-registered animations (captions, kinetic, credit, spotlight, brand-bug entrance) for the WHOLE render while framework-native <video>/<img> plates (data-start/data-duration, not tl-driven) keep playing back correctly -- a silent defect bl_checker.py's own empty-frame gate cannot catch when the underlying plate has real texture (my broll clips do). Not a beats.json content bug (same kinetic()/caption() shape rendered fine in Arm1's own earlier commit 0034f4d7/cef48665). Re-rendering.
- 2026-09-25T19:05:00Z second render (identical beats.json, re-render to rule out a load flake) reproduced the exact same defect deterministically -- confirms this is a content bug, not contention. Root-caused with a puppeteer/chrome-headless-shell debug harness (node + the same chrome-headless-shell binary hyperframes itself uses) loading the composed index.html directly and listening for `pageerror`: CURIOSITY-3's single-line kinetic text ("...เหมือนกัน", 68 chars incl. markup) measured 1896px wide at the 32px floor against a 720px safe box (--safe-left:120/--safe-right:240 on the 1080 canvas) -- `fitKineticLine()` threw, which aborted the WHOLE inline <script>, so every kinetic()/caption() call after it (all 9 KIN beats + all 3 FF captions, since kinetic() calls precede caption() calls in emission order) never registered on `tl` -- exactly matching what QC saw. The `sub_timeline_readiness_timeout` WARN in both render logs was an unrelated, always-fires-at-45s red herring (this template has no actual sub-compositions), not the cause.
- 2026-09-25T19:10:00Z fixed: split every KIN beat's long line into 2-3 shorter lines (kept 1 line only for the already-short SUMMARY-1). Verified with the same puppeteer harness against a fresh --no-render compose: 0 pageerrors, all 25 wipe spans present, tlDuration=48.48 (matches), and all 9 blocks' bottom edge <=1136.9px (within the 1164px safe-bottom marker) -- fits before spending another 12-minute render.
- 2026-09-25T19:20:00Z third render (fixed line splits above) succeeded visually -- QC by eye at 9 timestamps: every kinetic beat and every caption now shows correctly, brand-bug entrance animates in too (it's tl-registered, right after the earlier fix). BUT `tools/bl_checker.py` still failed: a `kinetic_overflow` gate (task-9a4f1029, checker-side, static per-char width ESTIMATE at each line's AUTHORED font-size, deliberately ignoring the render-time shrink-to-fit -- "a beats.json author should split a too-long line rather than lean on the render-time shrink") flagged 13 of my bl-lg/bl-xl lines as over the 720px safe width even though they visually fit after runtime shrinking. This is a real, intentional design gate (not a false positive), so fixed for real: recomputed max visual-chars/line per class from the checker's own KINETIC_CHAR_WIDTH_RATIO (bl-xl~12, bl-lg~15, bl-md~20, bl-sm~28), switched every long KIN body line to bl-sm (was bl-lg), kept bl-xl only for the 1-char checklist numerals and bl-lg for the one already-short SUMMARY-1 line. Verified against the checker's own `check_kinetic_overflow()` directly on the dry-run compose (0 flags) before re-rendering, plus the same puppeteer harness (0 pageerrors, all 9 blocks' bottom edge <=1048px, still within the 1164px safe-bottom).
