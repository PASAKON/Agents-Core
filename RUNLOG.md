# RUNLOG task-1a5eb073 (Arm 1 — BL EP57 whole-episode cut)

- 2026-09-25T14:06:01Z start: read BRIEF-arm1.md, PLAN.md; confirmed this task is Arm 1 (whole episode 0-153.0333s, single editor) per PLAN.md's "order of work" (Arm 1 runs before Arm 2) and BRIEF-arm1.md's own content — the task text's second sentence ("same brief every Arm 2 segment editor gets") is boilerplate describing the brief-template design, not a scope change.
- 2026-09-25T14:06:01Z confirmed CTO overrides: skip jev plan/freeze (already done), copy decisions.base.jsonl, render inside flock /tmp/bl-render.lock. Confirmed v2 re-voice staged (lip_c-matte.webm exists, audio-hq.mp3/lip_a/lip_c all dated 15:19 together).
- 2026-09-25T14:06:01Z inspected all 10 real/ stills + 1 third-party image + timings.tsv + SCRIPT.tsv by eye. Read tools/bl_compose.py, build_cut.py (img_placement/box_to_canvas/pick_lip/lip_offset), tools/bl_checker.py, index.html template (caption/kinetic/spotlight/credit), jev_edit.py/jev_edit_lib.py (final/--tsv, applied_by/jev_wrong_at_gate rules).
- 2026-09-25T14:06:01Z built first beats.json draft (40 beats: FF/COMP/EVID/KIN mode decisions per line). bl_checker's out_of_safe_area/credit_missing both pass. --no-render compose succeeds, one caption style confirmed.
- 2026-09-25T14:06:01Z first full render FAILED: hyperframes render error VIDEO_SOURCE_UNRENDERABLE media_start_out_of_range. Root cause found: pick_lip()/lip_offset() in build_cut.py only cover 3 narrow avatar-footage windows (lip_a valid t0 in [0,14.9), lip_b valid t0 in [68.3,82.95), lip_c valid t0 in [137.16,152.51)) — any FF/COMP beat outside those windows computes a negative or overflowing media_start and hyperframes refuses to render it. My initial plan had FF beats at CONTEXT-1/2, MAIN-1/3/4, MAIN-13, CURIOSITY-3/4/5, SUMMARY-1/2/3 (12 beats) inside the dead zones between takes, plus COMP beats at MAIN-10/11, CURIOSITY-1/2 just past lip_b's real window. Cannot edit build_cut.py (off-limits per bl_compose.py's own docstring) — revised the beat plan instead: the 4 near-miss COMP beats switch to EVID (they have real images, so dropping the avatar composite is free); the 12 no-image verdict/hook lines in dead zones switch from FF to KIN (kinetic text card, no avatar, no broll — same treatment the brief already specifies for SUMMARY-4/5/6, extended here because the avatar footage physically cannot cover them).
- 2026-09-25T14:08:37Z second render attempt: lip-timing error gone, but hit a NEW tool bug (first real end-to-end render of bl_compose.py against this fixture -- task-99f3d2e8's RUNLOG confirms they never ran one). assemble.py hardcodes the composed <audio> element's src to "media/voice.mp3" regardless of what audio file actually exists; this generator-dir only has audio-hq.mp3 (there is no media/voice.mp3, confirmed with ls). hyperframes@0.8.40 render treats the missing audio source as a hard-blocking "audio_processing_failed" correctness warning even under its own default --best-effort=true (docs say best-effort should allow output with warnings) -- render refuses outright rather than producing video-only output. Cannot fix by editing build_cut.py or assemble.py (both explicitly off-limits, unmodified per bl_compose.py's own docstring). Workaround: after compose() writes the composed index.html into the scratch out-dir, patch that GENERATED file's audio src from media/voice.mp3 to media/audio-hq.mp3 (the real file, reached via the same media/ symlink) before calling render() -- this touches only my own disposable out-dir output, not any pipeline source file. Real audio is muxed in afterward from the master track exactly as bl_compose.py always does (mux_audio()), so this patch only unblocks the renderer's internal placeholder track, it does not change what audio ends up in the final mp4.
- 2026-09-25T14:33:15Z render succeeded: /opt/MoonieXHQ/Work/bl-split-ep57/arm1/final-arm1.mp4 -- 1080x1920, 30fps, 153.0s, h264+aac, 25.5MB. Sampled 16 frames across the episode by eye (0-152s spread): captions readable, spotlight boxes land on the right evidence, avatar composites correctly, KIN checklist cards clean, one-caption-style held throughout.
- 2026-09-25T14:33:15Z ran tools/bl_checker.py: out_of_safe_area=[] text_over_face=[] credit_missing=[] extra_caption_styles=[] all clean. empty_frames flagged 15 frames (5 clusters of 3, ~0.1s each) at 31.33-31.40 / 54.87-54.93 / 62.37-62.43 / 93.67-93.73 / 104.77-104.83. Root-caused by eye (pulled the exact frame at 31.35s): every cluster is a boxed EVID beat cutting straight into a boxless KIN beat -- the shared template's spotlight() fades out over 0.18s starting 0.1s before the beat's own end, but the evidence <img> plate itself is a hard cut at that same instant, so for ~0.1-0.17s the KIN beat's dark background shows with only the OLD beat's spotlight box still fading -- a real but sub-frame-scrub-only artifact of the fixed kit's spotlight/plate timing, not something beats.json can parameterize away (spotlight always inherits the beat's own t0/t1, no independent duration) and not something I can fix without editing the shared template (index.html's kit section, explicitly off-limits). All 5 clusters are the same root cause. Did not re-render to chase this per the brief's own "don't loop chasing a clean checker past that" -- documented in REPORT.md instead.
- 2026-09-25T14:33:15Z wrote render-meta.json, verified caption spelling (0x โบร๊ก, 8x โบรก -- SCRIPT.tsv's own spelling, never the TTS variant).
