[Unit]
Description=Run the Claude usage monitor every 4 minutes

[Timer]
OnBootSec=2min
OnUnitActiveSec=4min
# Catch up a missed run after the box was down (e.g. reboot).
Persistent=true

[Install]
WantedBy=timers.target
