# `scripts/`

Repeatable setup, migration, launch, validation, maintenance, and diagnostic commands.

- Scripts must be safe to rerun and should expose destructive actions explicitly.
- Generated output and live process state must go to the configured runtime root.
- Cross-platform behavior should be separated clearly when commands differ.
