# `windows/`

Windows-specific launch, service, worker-node, and operational support.

- Keep shared business logic outside this directory.
- Runtime state must use the configured runtime root, not the source tree.
- Document parity or intentional differences from macOS/Linux operations.
