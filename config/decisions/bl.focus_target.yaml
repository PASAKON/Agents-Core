# Decision site: WHICH computed candidate box is the focus target?
# (task-5cfe20b1). Positional slot site — code computes the candidates
# upstream (jev_edit.py's dom_candidates_for_tag(), DOM boxes pulled from
# REAL_MANIFEST.json's `evidence_box` entries covering this line's tag,
# labelled A-F in reading order); Jev only picks among them. Only asked
# when bl.focus_device answered something other than none/other AND at
# least one candidate exists for this line.
site: bl.focus_target
question: >-
  Given the numbered candidate evidence boxes below (each one a region on
  the real-footage plate this line refers to), which one is what this line
  is calling out?
options:
  - id: A
    meaning: "The candidate labelled 'A' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: B
    meaning: "The candidate labelled 'B' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: C
    meaning: "The candidate labelled 'C' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: D
    meaning: "The candidate labelled 'D' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: E
    meaning: "The candidate labelled 'E' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: F
    meaning: "The candidate labelled 'F' in state.candidates — pick it if it is the region this line's evidence is about."
  - id: other
    meaning: "None of the listed candidates is the right region (including when there are more than 6 real candidates and the right one was not in the labelled list)."
max_state_chars: 1500
provider_policy: rules-then-llm
rules: []
counterfactual:
  images: 0
  big_model_tokens_out: 15
