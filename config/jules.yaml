# Jules dispatch allowlist (CEO 2026-09-23, skill jules-ops §1.1).
# The Jules GitHub app may SEE every repo. It may only be DISPATCHED to — and a PR from it
# only merged in — a repo listed here. Add a repo = a one-line PR the CTO merges.
# prod_adjacent: true → a merge still needs the CEO's per-repo OK (jules-ops §4).
model_default: "Gemini 3.6 Flash"   # CEO 2026-09-23; Pro only after a measured Flash failure
repos:
  - {repo: PASAKON/Agents-Core,        prod_adjacent: false}
  - {repo: PASAKON/MoonieX-ClaudeFlow, prod_adjacent: true}
  - {repo: PASAKON/MoonieX-WebApp,     prod_adjacent: true}
never:   # visible to the app, never dispatched to, whatever a brief says
  - PASAKON/Agents-Memory
