# Copy real values from "/Users/gob/LungNote Projects/webapp/.env.vercel.tmp"
# (NEXT_PUBLIC_SUPABASE_URL -> SUPABASE_URL, SUPABASE_SECRET_KEY -> same).
# NEVER commit .env — service key grants full DB access.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
LUNGNOTE_USER_EMAIL=pass.gob1@gmail.com
# Real dashboard account (LINE login). Email lookup unreliable — shadow user bug.
LUNGNOTE_USER_ID=
