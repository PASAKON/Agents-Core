# `data/` — legacy mixed content

This directory is not an approved shared data lake. Existing content must be assigned
to an owner before it is moved.

- Private knowledge goes to `../mooniex-private`.
- Project datasets go to `../projects/<project>` or managed storage.
- Runtime databases and caches go to `../runtime`; do not add new data here.
